# Build output
/bin/
/dist/
*.exe

# Local env / secrets — never commit
.env
*.pem

# Go
/vendor/

# Editor / OS
.DS_Store
.idea/
.vscode/

# Brainstorming visual companion (superpowers)
.superpowers/

# AI-planning scratch (superpowers skill output); never commit these
docs/superpowers/

# Web (Astro) build + deps
src/web/site/node_modules/
src/web/site/dist/
src/web/site/.astro/

# Canonical pricing is injected from the private poddle-cloud repo at deploy
# (scripts/sync-pricing.mjs); never commit the generated file.
src/web/site/src/data/pricing.generated.json

# Dashboard SPA deps (the built bundle IS committed at src/cli/dashboard/dist)
src/web/dashboard/node_modules/
src/web/dashboard/test-results/
src/web/dashboard/playwright-report/
src/web/dashboard/.last-run.json

# Site e2e (Playwright) artifacts
src/web/site/test-results/
src/web/site/playwright-report/
src/web/site/.last-run.json

# Agent scratch / parallel git worktrees
.claude/worktrees/

# Root dev tooling (git hooks / commit linting)
/node_modules/
lefthook-local.yml

# MCP browser artifacts (Playwright)
/.playwright-mcp/
